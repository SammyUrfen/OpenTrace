{
  "name": "opentrace-electron",
  "version": "0.0.1",
  "description": "OpenTrace Electron shell (main process)",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "rebuild": "electron-rebuild -f -w node-pty",
    "postinstall": "electron-rebuild -f -w node-pty"
  },
  "devDependencies": {
    "@electron/rebuild": "^4.0.4",
    "electron": "^42.0.1"
  },
  "dependencies": {
    "node-pty": "^1.1.0"
  }
}
